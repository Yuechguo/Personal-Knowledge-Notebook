
# 源码地址
``` shell
https://github.com/ROCm/AMDMIGraphX
```

# 运行脚本
``` shell
migraphx-driver perf Qwen2.5-0.5B-ONNX/onnx/model_fp16.onnx --fill1 input_ids --fill1 attention_mask --fill1 position_ids --dim-param "@past_sequence_length" "1" --input-dim @input_ids 1 <sequence_length> --input-dim @attention_mask 1 <sequence_length+1>
```