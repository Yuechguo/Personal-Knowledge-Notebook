How to run container with specifical mi-gpu index:
![](./Pasted%20image%2020240902144222.png)

if with cpx mode, run container with device flags(4 cpx mi-gpus):
``` shell
docker run -it --network=host --device/dev/kfd --device=/dev/dri/renderD128 --device=/dev/dri/renderD129 --device=/dev/dri/renderD130 --device=/dev/dri/renderD131
```
### TF115 build with Ali-ROCM6.3 
#### fake one file for comiple
``` shell
touch /usr/include/sys/sysctl.h
```
### fix rocm clang version issue
``` shell
inc_dirs.append(rocm_config.rocm_toolkit_path + "/lib/llvm/lib/clang/18/include")  
inc_dirs.append(rocm_config.rocm_toolkit_path + "/llvm/lib/clang/18/include")
```
#### temp block dropput op compile
``` shell
cc_library(
    name = "nn",
    deps = [
        ":batch_norm_op",
        ":bias_op",
        ":conv_ops",
        ":data_format_ops",
        ":depthwise_conv_grad_op",
        ":depthwise_conv_op",
        ":dilation_ops",
        #":dropout_op",
        ":fused_batch_norm_op",
        ":in_topk_op",
        ":l2loss_op",
        ":lrn_op",
        ":nth_element_op",
        ":relu_op",
        ":softmax_op",
        ":softplus_op",
        ":softsign_op",
        ":topk_op",
        ":xent_op",
    ],
)

#tf_kernel_library(
#    name = "dropout_op",
#    prefix = "dropout_op",
#    deps = NN_DEPS + [
#        ":random_op",
#    ]
#)
```